// Interpret only the student's utterance, never instructions in an attachment.
export function needsSetupPicture(student: string) {
  return /\b(?:i(?:'m|’m| am) (?:totally |really )?(?:lost|confused)|(?:don(?:'t|’t)|do not) (?:really )?(?:understand|get) (?:anything|this(?: problem)?|the (?:setup|problem|question)|what(?:'s|’s| is) happening)|(?:can(?:not|'t|’t)) (?:picture|visuali[sz]e)|(?:explain|describe|show me|help (?:me )?(?:understand|picture)) (?:the |this )?(?:setup|situation|problem)|(?:don(?:'t|’t)|do not) know (?:where|how) to (?:start|begin))\b/i.test(student);
}

export const SETUP_PICTURE_HINT = `The student needs orientation to the situation, not another demand for an equation or an attempt. If a specific problem is identified, first describe what is happening in one plain sentence: the objects, what changes, and what the question is asking. Then compose one small labeled DRAW diagram of that situation before asking one concrete noticing or prediction question. Prefer spatial relationships, stages, arrows, or a qualitative graph over a block of formulas. When change over time is the confusion, use a short ANIM with a clear thing to watch, without revealing the requested result. This is setup support, not a calculation or complete solution. Do not ask what they have tried again after they have said they cannot understand the setup. If no problem has been identified, ask which one first; do not choose an assignment question automatically. A diagram must contain meaningful geometry, not just a title or equation. Keep it general to the actual subject; no scripted scenes.`;
