# Better Office Hours

A voice tutor that knows your course and talks you to the answer instead of handing it to you.

Where we are: `docs/STATUS.md`. Agents start with `AGENTS.md`. Product spec: `docs/DESIGN.md`.

Run locally: `npm install` then `npm run dev`, and open [http://localhost:3000](http://localhost:3000).

(Full README structure is in `docs/DEMO.md`. Fill in before submission.)
