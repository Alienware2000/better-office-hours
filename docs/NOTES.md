# Build notes

Each lane keeps a few lines here: what works, what is stubbed, what other lanes need to know. The accumulated voice/workspace/whiteboard work is now the approved main baseline. Historical lane names below describe where work originated; new work starts from main. Read LANES.md before Hussein starts a slice.

## voice

Merge approval: David authorized merging his PRs #5 and #7. PR #7 is merged; its documentation overlap was reconciled into #5 without implementation changes. Hussein's #6/#8/#9 are explicitly excluded and remain open for later review. Continue the fresh diagram task from the integrated baseline.

Visual continuity (2026-09-11): visual tags attach to the next audio onplaying event, including delayed synthesis/browser buffering; cancelled or replayed playing events cannot duplicate ink. Structured animation resume/focus survives conversion and current-scene controls avoid static repair. Deep requests use 30s initial / 20s idle / 60s total deadlines, tested with a fake clock beyond the old cutoff. Development request IDs connect counts/latency to applied revisions; no transcript or audio in these logs. Actual rocket-PDF Chrome flow generated a setup and a later in-place moving rocket. TTS was stubbed, input synthetic through real Silero, models real. First teaching/motion began about 10s/21s after acknowledgements. Do not claim low latency or perfect model pedagogy: a fast acknowledgement still asked an unnecessary question. An earlier test was disrupted by source-edit HMR; the older cliff fixture was also not the rocket PDF. Final validation used the correct PDF and a stable build.

Concept lessons (2026-09-11): `concept-routing.ts` uses a small semantic JSON decision on the existing fast model; substantive lessons/checks use the existing reasoning lane. `boardContextForTurn` carries real store availability/provenance before the first preview mounts, so a spoken concept entry does not look like a lobby request. `concept-response.ts` releases complete narrated beats progressively and translates them into the existing TEACH/DRAW/ANIM/PDF stream. It shares drawing normalization and animation validation, preserves disclosure/anchor checks, and never generates a topic fixture. `check-concept-lessons.mjs` covers every character boundary, escaped JSON/LaTeX, early beat release, narration ordering, exact duplicate speech, malformed/truncated data, routing, cancellation, and equation restraint. Build/focused lint and existing voice/teaching/diagram/workspace/animation checks pass. [xAI structured-output transport](https://docs.x.ai/developers/model-capabilities/text/structured-outputs) is internal; no shared contract changes.

Rejected concept experiments: guidance-only fast replies still omitted visuals; fast structured teaching emitted text-only recursion and inconsistent projectile velocity/motion. Stopped using that model for substantive concept teaching rather than adding topic-specific fixes. Direct reasoning-first probes took about 25s before speech and up to 31s to finish, so the fast acknowledgement remains. The final live Chrome test took about 12s from acknowledgement to substantive teaching, then drew three narrated stages with one question and no equation. A separate real homework setup probe finished in about 20s and emitted an orienting two-stage sketch without a formula or calculated answer. Spoken entry from the lobby passed using the recorded generated response. These are qualitative samples, not general physics validation. An earlier browser probe was invalidated by its looping synthetic microphone interrupting the pending lesson; muting the test feed after its first STT capture allowed the full lesson to run. Do not disable real barge-in to make that test pass.

Listening follow-up: the latest hardware report is still Listening with no reaction. Active development sessions emit safe five-second energy/probability peaks and capture state; peaks catch utterances between log samples. No audio, transcript, device name, or credentials enter these traces. The user's tab was paused during inspection, so there is no new failure trace. Real Silero with synthetic Chrome input again recovered after rejected/failed STT. Next capture the actual failing tab's trace before changing thresholds or asserting another microphone fix.

Listening recovery (2026-09-11): AudioContext interrupted previously fell through both input guards. Check ended/closed tracks and muted/disabled input separately from VAD heartbeat; resume briefly interrupted audio, then expose Retry after 2s if unavailable. Retry starts the replacement mic and preserves the current desk/history. Safe fault diagnostics include state and detector frame age only. Lifecycle checks now exercise actual next-turn recovery after noise, empty/error/timeout STT, mishearing, and late responses. Chrome injected microphone faults and verified one-click recovery without lost notes or a repeated greeting. A real Silero browser run accepted the third captured utterance after injected noise and STT failure. Exact real-device cause remains unconfirmed. See [browser audio states](https://developer.mozilla.org/en-US/docs/Web/API/BaseAudioContext/state).

Authentication recovery (2026-09-11): ElevenLabs returned 401 invalid_api_key with detail.status and detail.code. Replacing the ignored local key restored real conversational v3 synthesis and Chrome playback. TTS now maps structured provider categories to safe client errors; specific quota/permission statuses take precedence over generic unauthorized codes. Logs contain status/category/model only. check-tts.mjs loads the actual route/helper and covers these failures, malformed input, missing config, network/cancellation, secret-free diagnostics, and unchanged model parameters. Build, focused lint, and lifecycle checks pass. Other environments manage their own credentials.

Teaching intent follow-up: local [TEACH move=... visual=...] metadata comes from the model's existing turn, not phrase matching or a separate planner. Elicit/orient withhold symbolic relationships on the board; hints/consolidation/explanations may show them. Silent recovery inherits the parent's move, with the existing 6s/cancellation limits. Removed speech formula copying and lexical setup/repair triggers. Default hint counters are now explicitly unavailable rather than resetting to zero. Live probes covered first recall, requested reminder, and differently worded incline/recursion visual requests. Stage selection and spoken restraint still depend on model judgment. `scripts/check-teaching-intent.mjs` covers stream/recovery disclosure and is required alongside the lifecycle checks. Human PROMPT/PEDAGOGY and shared types are untouched.

Capture now uses the pinned Silero frame callback's 16kHz PCM, with 320ms pre-roll in RAM and bounded WAV encoding. Pause/discard purges the buffer. This replaces speech-triggered MediaRecorder startup, which could miss opening sounds. Ordered STT continuation and cancellation tests still pass; Chrome with generated speech verified the actual VAD/capture/Scribe path. Real mic/echo quality is still unverified. STT receives up to 24 vocabulary-only hints, not expected answers; [ElevenLabs keyterms](https://elevenlabs.io/docs/api-reference/speech-to-text/convert) carry a 20% surcharge. TTS page/problem references are spelled out while decimals remain intact; the live "page 1." round trip returned "page one".

Continuation repair: the recorder may start during pending STT. A cancellation-scoped batch preserves fragment order and waits for continued recording before sending one joined utterance. Failed batches do not send partial text. Quiet endpoint is 1.5s, with manual finish retained. Lifecycle tests cover both STT completion orders, pause/late results, and weak noise. Hardware pacing remains a manual check.

Current endpoint uses clear speech (probability >= 0.6) to extend a confirmed recording, while the lower gate still retains quiet syllables. Approximately 1s without clear speech submits. Suspended AudioContext retries resume and exposes microphone retry after 2s. Playback uses rate 1.08 with preservesPitch. Test with quiet voices: this is acoustic endpointing, not semantic turn detection.

Readiness now permits a fixed one-time "I can see your PDF now." receipt after 1.5s of idle quiet. No LLM call, problem selection, or question is allowed for this receipt. Busy uploads stay silent; starting speech cancels the pending receipt, as do changed pages/session ownership and its 8s expiry. Existing live snapshots supply the attachment on the next actual student turn. Lifecycle harness covers these rules and suspended-input recovery.

v3 integration gotcha: previous_text/next_text are rejected by the provider, despite ordinary TTS parameter documentation listing them. The earlier one-sentence smoke test missed this. Only Flash receives previous_text now. scripts/check-tts.mjs exercises the actual route with both models. Three sequential live requests passed. Keep sentence playback boundaries for interruption/history integrity until audio alignment is available; a grouping experiment was withheld after the lifecycle regression caught unplayed words in history. Tone softening is runtime-only and still needs David's listening review.

Latest voice/board pass: default TTS is eleven_v3_conversational, with stability 0.5 and an explicit Flash rollback via ELEVENLABS_TTS_MODEL. The quota initially blocked synthesis; after David upgraded, direct synthesis, STT round trip, and actual browser decoding passed. Requests surface quota errors clearly. This is not ElevenAgents managed turn-taking: local VAD and batch Scribe v2 remain. No key values are stored in docs.

Teaching surface: BoardText shares symbol colors and math font choices with snapshots and ANIM. General text lines allow 64 characters, arrow labels stay short. Opening the board after student speech does not seed content. MODE tags only enter a desk from the lobby. Local LivePage adds optional measured textRegions, passed through the voice route for highlighting; shared lib/types.ts is untouched. Browser anchor test initially selected a nonexistent ninth fragment in its synthetic fixture twice; corrected the fixture target, then zoom/resize checks passed. No test routes remain in the app.

Earlier follow-up: Scribe v2 is verified through the actual STT endpoint with generated physics speech; Flash v2.5 remains TTS. The full current-session transcript is scrollable/exportable with Latest to resume following. Export contains captions, so an interrupted sentence may still be included in full. No durable transcript storage is implied.

Voice lifecycle follow-up from `lane/voice-polish`, approved for main in PR #3: STT has an abort controller plus a playback-generation check so even an abort-ignoring late response cannot reactivate a paused session or reach another desk. Orb enters thinking when recording stops; noise/failure recovers to listening. Enter, Leave, and Remove discard unfinished input and restore microphone/orb state. `scripts/check-voice-lifecycle.mjs` runs the actual hook with deferred STT and a simulated microphone across pause/resume, desk changes, pagehide, and unmount. Real mic cadence and hands-free interruption remain follow-ups.

Current stress-test repair is on `lane/voice-stress-fixes`. The custom STT/Grok/TTS loop remains. The second stress pass adds pinned local speech detection; see the update below. Attachments and settled ink only update context; `sendEvent` keeps its readiness callback signature but intentionally starts no model turn. A live prompt-only upload probe still selected problem one, so this is enforced in the client rather than entrusted to wording. The student sets the agenda by voice.

The second stress pass replaces RMS-only triggering with local Silero v5 speech probabilities (`@ricky0123/vad-web` 0.0.30). ONNX Runtime uses one WASM thread. The analyser only drives the orb meter. Startup has a visible preparing state and a retry button on failure; detector heartbeat failure also exposes retry. `npm run dev` and build copy the installed model/worklet/WASM to ignored public assets. This is a documented shared package change for the voice repair; Hussein should run npm ci after pulling it.

The endpoint waits about 1.1s of non-speech. The orb starts or submits, never cancels, so an automatic-endpoint/finish-tap race cannot discard the response. Pause and Escape stop voice; Escape no longer leaves the workspace. The touch Pause target is separate from the orb. Recording completion/STT/TTS/generation have 3s/12s/15s/30s deadlines, and startup has a 20s deadline. No missed response should own the floor indefinitely. Speech classification is not speaker identification: vocals or background conversation can still trigger it. Real iPad audio, music, and quiet voices remain hardware checks.

A headless Chrome smoke test loaded the actual local model and worklet, rejected its synthetic non-speech input, verified Pause and Escape, and checked the 768px touch layout. The hook harness separately covers primary-tap races and abort-ignoring request timeouts. A live reminder probe produced four short written kinematic relationships plus a question, but took about 9.2s to first token in the reasoning lane.

All generated speech is queued, with no hidden three-chunk/70-word cutoff. Upcoming audio is prepared while the current chunk plays. Captions and assistant history advance at sentence playback start, so cancelled future sentences never become conversation history. The currently interrupted sentence can still be partially spoken; there is no word-alignment service. Decimal boundaries are preserved during streaming. API requests and queued visuals are cancelled with the turn. TTS-only notation expansion lives in `lib/agent/spoken-text.ts`, preserving symbols in captions and board data. General repetition reduction is a runtime instruction, not proof of human-sounding delivery.

Fast model stays `grok-4.20-0309-non-reasoning`; reasoning stays `grok-4.6`. JSON tags are never sent to TTS. Active session history is sent each turn, and the full active session is available in scrollable captions. Explicit topic-switch language opens the concept desk. The tutor sees current PDF and board snapshots on the next voice turn, including student marks.

Two lanes live in `lib/agent/grok.ts`: `GROK_MODEL` (fast, non-reasoning, every turn) and `GROK_DEEP_MODEL` (`grok-4.6`, `reasoning_effort: "low"`, `max_tokens` 2400 because reasoning tokens count against the cap and a tight one returns an empty message). The route takes `deep: boolean`. The lane instruction must stay last in the system message; from higher up the fast lane ignored it and answered hard problems itself, and a vague version of the instruction did not work either, so `WHEN_TO_THINK` names the trigger cases and shows an example lead-in. All spoken chunks go through one queue (`enqueueSpeechTask`), because the lead-in and the reasoning reply are produced concurrently and would otherwise play over each other. The client no longer caps spoken output. Shortness belongs in the tutor instructions; the previous cap silently lost closing questions.

Captions are in `app/(session)/voice/Captions.tsx`, deliberately not `components/transcript/`, which belongs to the shell lane. Do not move or replace these components without a coordinated PR with David. `useVoiceLoop` returns `turns: Turn[]` and revises the last tutor line as the reply streams. The panel keeps all spoken lines, allows selection and export, and follows new speech only while the reader stays at the bottom. Putting the paper away (`putAwayPset`) clears the homework history the model was using, not just the on-screen lines.

Leave parks a **session**, not just a view. There are three kinds: lobby (the orb waiting room), homework (this pset), and concept. `exitWorkspace` snapshots homework/concept history and captions, then resets to the greeting so lobby talk cannot keep asking about the PDF. `enterWorkspace` and saying homework restore the parked homework session. `putAwayPset` throws the paper away instead of parking it. "Explain a concept" from the lobby does not keep the pset transcript or `livePage`. Bracket-only STT (`[background noise]`) is dropped in `speech.ts` before it becomes a turn.

Open pedagogy risk with the non-reasoning model: on a page question it said "First thing is to split the launch velocity into x and y pieces" and then asked "What do you think the first step is?", which is rung 3 of the hint ladder followed by rung 1. PROMPT.md says never open at the bottom. This is the tradeoff for sub-second speech and is why the reasoning-lane split matters. It also invented a table on a cliff problem. Anything that needs to be right about the work should go through `[THINK]`.

## workspace

Toolbar density follow-up: PDF controls now stay in one pill row with horizontal scrolling when the desk is too narrow; no wrapped page-navigation row. SVG history arrows replace font-dependent glyphs. Chrome checked vertical bounds and one-row alignment at 1400/1000/700px.

Ink editing follow-up: Overlay uses pointer capture, coalesced samples, rAF previews, shared quadratic paths, and segment-distance erasing. A gesture commits once; cancel leaves no partial ink. PDF annotation history is bounded to 30 edits, resets on document load, and stays with the mounted viewer when changing views. Snapshot strokes use the same curves with pixel-space width scaling. Browser checks passed for sparse-stroke erasing and undo/redo. Focused Overlay lint is now clean; five existing PdfViewer render-ref findings remain.

Cursor/highlight follow-up: familiar fixed-size arrow cursor now sits 23px before anchored text; unanchored POINT keeps its exact tip coordinates. Text highlights use the full region with a lighter translucent fill, replacing the strike-through-like narrow path. Browser checked clearance at 100%/125%; temporary fixture route removed. Old chevron notes below are historical.

Toolbar/guidance repair: PDF controls no longer flex-shrink and clusters wrap within the desk. Pointer is now a fixed-size margin chevron without a label/glow; highlighting also shows it. Text targets use measured PDF bounds and scroll to the passage start. `[HIGHLIGHT page=N anchor=ID]` resolves against the request's LivePage textRegions and enters the existing speech queue; invalid/page-mismatched IDs are ignored. Browser verified control bounds, zoom, resize, and off-text cue placement. Legacy PdfViewer render-ref lint findings remain.
Lecture mentions no longer route homework into a separate concept session. This reproduced the reported disappearing PDF: it was parked, not removed. Regression phrases include the actual lecture correction, teaching a term, and talking about initial velocity. Explicit topic switching and the dedicated Explain a concept request still change sessions.

Stress-test follow-up: PDF navigation scrolls the desk container to reveal the pointed or highlighted region only when offscreen, instead of centering the whole page or moving the document body. Coordinates use measured page rectangles after zoom, and the pointer continues to track scrolling. Caption autoscroll stays inside its own panel. Model localization accuracy and cursor aesthetics still need human review.

On `lane/workspace`, branched from `lane/voice`. Drop zone after Homework / `[MODE pset]`. Upload is `POST /api/pset/upload`, files in `.data/psets` (gitignored). Viewer is PDF.js in the browser. Pointer eases; tutor highlight is a marker; student ink is freehand. Current page JPEG (max 1024px) is posted with the LLM turn so Grok can emit `[POINT]`. The tutor's pointer is `components/workspace/Pointer.tsx`: the head eases toward the target on every frame and drags a fading tail, drawn as separate segments in pixel space. Do not go back to a gradient-stroked path, which fades along the page's x axis rather than the direction of travel, and do not go back to a CSS position transition, which teleports. The highlight is a marker stroke that sweeps left to right under `mix-blend-mode: multiply`, keyed on the bbox so a new highlight replays the sweep instead of sliding. Both honor `prefers-reduced-motion`.

The visible page now drives `livePage` through an IntersectionObserver, so the tutor sees whatever the student scrolled to. It used to update only when the tutor pointed, which left it blind to scrolling. The pset is held by `VoiceSession`, not `WorkspacePane`. Leave parks the desk instead of unmounting it, so the PDF, scroll, and ink are still there on return. A parked desk passes `active={false}` so it stops publishing `livePage`; otherwise Grok kept seeing the pset from the lobby. The Homework chip restores a parked desk without sending another utterance. `Leave` is on the workspace bar; Escape does the same. Do not overlay the welcome stage on top of an open desk: that trapped clicks on Next and Leave. The welcome stage unmounts when the split is open. The laser is one overlay on the visible page frame, not a per-page mount, so it can travel between problems. The first point arrives from slightly below the target; later points continue from the last head, including across remounts. The tutor highlight is an irregular marker stroke (`Overlay` `Marker`) with a left-to-right sweep and multiply blend, keyed on the bbox so a new region replays the sweep.

Student ink is the second bar: hand, pen, highlighter, eraser, and black / rust / gold (`InkBar`). Zoom and page stepping sit in the same pill. Default 100% fits the desk column on every viewport; − / + zoom around the center of the desk, pinch or Ctrl-scroll zoom around the cursor, and the hand tool drags to pan. Click the percent to fit again. Range is 75% to 250%. Do not scale the page width without pan: that cropped the paper. Default tool is hand so the page still moves. Strokes stay in the viewer and are composited onto the vision JPEG (`paintInkOnImage`). Do not send freehand ink through `StudentAnnotation` in `lib/types.ts`. Drag-to-circle / underline is gone; it felt unlike a notebook. **Remove** on the title bar puts the PDF away and starts a fresh homework session (drop zone, empty captions). This is not a notebook library. Two CSS-only transition attempts felt janky in human review; replaced them with Motion `layoutId` for the orb and independent workspace entry. Do not restore the old transform/width choreography. Missing-key mode still opens Homework without calling the agent. Breakpoints: workspace-heavy split on desktop, stacked agent/workspace at 640px. The ink bar sizes down with the desk via a container query. Dedicated mobile design is later.

## whiteboard

Fresh-task handoff: David authorized continuing diagram mechanics and demo polish after 5cef623. Preserve the working voice/board pipeline and current paper UI; prioritize coherent movement, clear annotations, smooth progressive construction, and general model-selected visuals. STATUS records validation and remaining latency/model-judgment limits.

Scene continuity (2026-09-11): same animation ID updates the current page; a new animation reusing a static geometry ID replaces only those objects and preserves the backdrop/ink/labels. Dependencies on a replaced static object become unresolved rather than remain falsely attached. New unrelated scene IDs still archive. LiveBoard includes validated animation spec/time/playing for follow-up focus/replay. Static cubic paths now pass through declared points with bounded controls; animated paths retain their sampled-polyline/Follow semantics. Dot fills match static bodies. Regression checks cover source-to-animation IDs, revisions, labels, missing controls, ink, provenance, curve extrema, and prior animation behavior. Broader diagram sophistication and physical accuracy remain model-dependent.

Diagram composition: optional local diagram metadata adds explicit contact/attachment relationships, hatching, body fills, weight, and label side. `diagram-compose.ts` resolves source commands by ID, retains dependency sources for later revisions, and withholds unresolved/cyclic/off-page results. Never guess contacts from labels. `BoardShape` shares styling with static and moving SVG; snapshot uses the same fills, heads, widths, and alpha. Contact is circle-to-line, attach translates line/arrow starts to existing anchors. Chrome and check-diagrams cover real generated incline/graph output, reactive geometry, ink, aliases, page scoping, reduced motion, and compact labels. First live probe flattened fields, now normalized; second correctly nested and attached. Physical interpretation remains the model's responsibility. No shared/human prompt edits.

Diagram revision easing experiment: CSS d transitions on SVG attributes, then explicit CSS path() values, both failed the Chrome intermediate-frame check. Those probes overlapped initial stroke reveal, so they did not establish support for settled-shape morphing. Removed the optional experiment after two attempts and moved on. Geometry updates atomically with its dependent shapes; first-draw stroke/fill reveal and label easing remain. Do not claim smoothly interpolated geometry revisions. The [SVG path specification](https://www.w3.org/TR/SVG/paths.html) also limits interpolation to compatible path structures; a general animation approach is a later slice.

LaTeX: `math-layout.ts` links MathJax Base/AMS and the bundled TeX font synchronously, using the [official direct-module approach](https://docs.mathjax.org/en/v4.1/server/direct.html). Pinned @mathjax/src and @mathjax/mathjax-tex-font 4.1.3 require npm ci on other machines. It extracts only path/rectangle primitives and transforms, never injects HTML; SVG and snapshot canvas share those primitives. Stored dimensions reserve real ascent/descent and progressive reveal follows glyph count. Base/AMS fractions, roots, indices, vectors, integrals, aligned rows, matrices, and legacy notation pass check-math plus Chrome compact/expanded/snapshot checks. Bounds/cache and unsupported-command fallback are intentional; this is not a general LaTeX document compiler. A live consolidation probe emitted a valid fraction. Human PROMPT/PEDAGOGY and shared types remain unchanged.

Diagram follow-up: local geometry traces support measured static label placement without changing shapes; preferred positions avoid cumulative drift. Annotations have a consistent smaller scale, lighter halo, and short positional transitions. The layout avoids existing paths, labels, and student ink when nearby room exists; it preserves labels with least overlap when crowded. Resolved marks feed SVG and snapshot. Moving labels retain their attachment/size and do not run collision avoidance every frame. Simple ASCII powers/subscripts and comma-separated given rows now typeset cleanly. Live rocket/pendulum traces and an incline/recursion response were rendered in Chrome; compact/expanded checks passed. Physical correctness remains model-dependent; this does not fix the earlier invalid pendulum motion attempts.

Writing/pages follow-up: `reveal.ts` schedules graphemes without layout reflow, wraps sequentially, and sets group completion timing; identical updates are idempotent. `BoardDrawing` shares rendering for current and archived pages. New topics/full writing areas archive content and student ink in `earlierPages`; animations and recovery diagrams get clear space. Current-page tools stay fixed around a shorter scroll viewport. Earlier pages are read-only references, not a notebook editor. Latest restores following; incoming pages respect readers scrolling back. LiveBoard adds current page, earlier topic summaries, and readingEarlier, without changing lib/types.ts. Browser plus ink/teaching/lifecycle regressions cover continuation, persistence, reveal, reduced motion, edits, and geometry recovery after a formula-only response. Setup guidance lives in runtime visual-help.ts, not human PROMPT/PEDAGOGY.

Motion probe limitation (2026-09-11): two live fast-model pendulum attempts returned an unsupported ANIM kind=line and inconsistent moving attachments, even after explicit schema/constraint guidance. Invalid specs remain rejected. Do not normalize those into a misleading animation or wire a hand-authored pendulum fixture. The bounded visual repair should provide a static setup sketch when motion cannot be represented correctly; broader semantic animation reliability needs a separate planner/evaluation pass.

Visible ownership/editing: Tutor notes and Your ink labels plus blue default student ink; hover an author label to emphasize that layer. `useBoardInk` handles select/box-select, bounded movement, smooth pen/highlighter, erasing, and cancellation. Palette/delete/undo act only on student strokes. Store history holds 30 edits, survives desk restoration, and never rolls tutor notes back. Tools sit below the square canvas; selection has a fixed-height row; expansion preserves marks. `check-ink.mjs` covers geometry/history; actual Chrome gestures verified edits, keyboard undo/redo, zero selection layout shift, view persistence, and 1200/900/700px tool bounds. Pressure-sensitive Pencil behavior and general note composition remain open.

Authorship is explicit in the local LiveBoard adapter: revision, tutor IDs/text/status, student count, separate student-only JPEG. Entering notes count as tutor-created even before the snapshot shows them. Never infer student authorship from the combined JPEG. Reserved `topic` text changes start a fresh tutor note; ordinary IDs update in place, and tutor clear preserves all student ink. Emit the topic first when changing phase, then rewrite current lines. This fixes stale givens under a new heading, but model choice of topic and updates still varies. No shared contract edits.

Writing measures browser glyph widths and wraps at additive/relational boundaries where possible. Continuations keep math typography and snapshot text shares SVG aspect scaling. Browser verification reproduced the full screenshot equation on one line in compact boards at 1440px/1000px, plus phase replacement with student ink preserved. Live probes updated stale tutor notes without student blame; this is a regression sample, not a guarantee of all future wording.

Missing-command repair: `speech-cue.ts` copies short general symbolic relations at audio start when the primary model omits DRAW; it does no calculation and rejects numeric assignments. Missing conceptual diagrams receive one visual-only LLM request with a 6s deadline, applied only in the current playback epoch. No extra speech, navigation, PDF action, clear, or remove can come from that lane. A repair can still fail or time out. `parse-draw.ts` normalizes circle `at`, point arrays, and rectangle aliases to existing DrawCommand types. Browser tests exercised no-DRAW primary responses through the real hook and SVG. No automatic fixture content.

Note composition: DRAW text IDs `topic` / `topic-*` select a muted heading; `given-*`, `note-*`, and `definition-*` select aligned rows. Equations retain centered, larger type. Stored local drawables carry alignment/heading metadata so SVG and snapshots agree. Shared types stay frozen. Runtime hints describe these recipes and require clear space between diagram geometry and note rows; arbitrary diagram collision avoidance remains unimplemented.

Writing repair: `writing.ts` wraps standalone DRAW text, reserves existing text/ink bounds, and stores resolved lines for SVG/snapshot agreement. No shared contract changes. Full-board writes are rejected without erasing student work; general diagram collision avoidance and pagination remain open. Screenshot overlap sequence is covered by teaching checks and inspected in both board sizes. Typography direction follows explicit size/position control in [Desmos labels](https://help.desmos.com/hc/en-us/articles/4405487300877-Labels), not a Desmos integration.

Voice tone: v3 stability 1 uses the steadier end of delivery, informed by [ElevenLabs stability guidance](https://elevenlabs.io/docs/best-practices/prompting). Actual synthesis passed. Keep the same voice and 1.08 playback rate for comparison; human tone review is still required.
Stress-test follow-up: board text supports short symbolic equations beyond the six-word label cap. Shared text sizing keeps SVG and vision snapshots aligned. Runtime hints request equations, general relationships, or incomplete examples when useful, not just graphs. Student ink remains intact and taking the board stops speech. A live general-concept probe produced `F = ma` plus one question; broader model reliability is not established.

Adaptive desk follow-up: `VoiceSession` renders the same grid for pset and concept; per-session PDF/Whiteboard view choice changes the content, not the shell. Homework defaults to PDF upload; concept and Something else default to the large board. PDFs retain their page/ink while switching views. Supplemental notes are held separately from homework and tagged as `documentKind: notes` in the local LivePage metadata, preserved by the route and runtime context. `notes_ready` reacts after rasterization and voice activation. Frozen `lib/types.ts` is unchanged.

Concept previously stayed in the lobby because `split` only checked pset. Paused tabs also claimed the voice channel at boot; that claim now occurs only for active voice. Intent covers common STT variants, and orb activation resumes suspended AudioContext state. Board state is included in parked work snapshots and restored paused; stale inactive PDF publishers only clear a page they own. Checks: `node scripts/check-workspace.mjs`, TypeScript, build, and browser entry/view/upload checks. Real mic and Apple Pencil feel still need hardware review. David approved publishing this branch; Hussein is the other team member.

On `lane/whiteboard`. SVG DRAW and declarative ANIM share `lib/whiteboard/style.ts` and geometry. The board uses a square drawing surface so vectors, circles, and labels keep their proportions. ANIM validates bounded data and supports all six shape kinds, linear/inOut/out keyframes, Follow, camera, focus, play/pause, and seek. Follow travels by sampled point index along a path's drawn fraction. New specs replace the animation layer; DRAW clear also clears it. No program execution or fixture selection by the tutor.

Voice queues complete DRAW, POINT, HIGHLIGHT, and ANIM beats after their preceding speech. Each model pass resets its visual cursor at the playback boundary. Stop, recording start, and drawing freeze motion; scrub pauses. Reduced-motion users get manual scrubbing without autoplay. `getLiveBoard` captures the visible frame on demand, including camera and student ink. PDF ink publishes its JPEG before an 850ms settled `student_mark` event. The voice loop no longer starts turns from these events. Fresh ink remains in the image for the next student-led turn. This cross-lane wiring was explicitly authorized for the slice; shared types and human prompt files are unchanged.

Dev helpers: `window.__bohBoard.playFixture(speed?, angle?)`, `loadAnimation`, `play`, `pause`, `seek`, `focus`, and `getState`. The projectile fixture stops at the peak and exists only for testing. Automatic URL fixture loading was removed after it populated a fresh homework desk. Old `?boardFixture=projectile` links now behave like normal entry. To test the fixture, explicitly call `window.__bohBoard.playFixture()` in development; mounting or remounting the board never seeds a diagram. Test with `node scripts/check-anim.mjs` against `:3100`, or `--unit` without a server. Unit coverage also includes a generic growing bar, text, and camera so the runtime stays topic independent.

Verification: build, TypeScript, and focused lint pass. Browser playback completes and keyboard scrubbing updates the frame. Live ANIM returned a valid general spec. First DRAW probe passed the tag-count threshold but used `op: draw`/`kind: arrow`; parser now normalizes that variation. Second DRAW probe emitted just one arrow across two turns, failing the script's two-tag threshold. Keep that model variability visible for human review. Full lint has existing ref-access failures outside the core whiteboard files. Live mic cadence and a real PDF mark reaction remain manual checks. David subsequently authorized pushing the completed slices.

## context
Hussein owns this lane. `lane/context` now has a pure first slice in `lib/context/*`: deterministic page-aware text chunking preserves course/document/title/page/storage/solution provenance, rejects malformed identity/page fields, and deduplicates repeated sources. Student retrieval filters by course and excludes every solution chunk, including other document kinds explicitly marked as solutions. The public student result projects only document kind/title/page/text, never course IDs, storage paths, embeddings, solution flags, or internal chunks; hidden references use a separate server-runtime-only function. `scripts/check-context.mjs` uses synthetic data to cover chunk limits, Unicode math, stable IDs, repeated ingestion, provenance, ranking quality, cross-course isolation, solution separation, result leakage, empty data, and browser rejection. There is no database, endpoint, embedding provider, Canvas access, or voice integration yet; those identity, ownership, API, and schema decisions remain explicit review items.

## recap
Hussein owns this lane. `lane/recap` is stacked on `lane/context`. `lib/session/recap.ts` validates and canonically serializes the existing `Recap` type; review references must be complete or honestly absent, and displayed fields reject explicit answer-key disclosures. `components/recap/RecapCard.tsx` shows only the sticking point, what changed, and a real review location, never the student's calculation or spoken transcript. `RecapRepository` proposes server-derived identity, course/session ownership checks, and a unique `(userId, courseId, sessionId)` upsert boundary so retries cannot duplicate cards. No persistence adapter, API, auth, or voice wiring exists yet; David coordinates the student-first spoken close flow after review.

## shell
Hussein owns this lane. `lane/shell` adds stable NextAuth v4, a Google route, Yale/judge email policy, normalized session identity, and an accessible `/sign-in` shell. Missing `GOOGLE_CLIENT_ID`, `GOOGLE_CLIENT_SECRET`, or `NEXTAUTH_SECRET` disables sign-in and names the missing configuration without exposing values. A dedicated Google Cloud project has an internal Yale web client with `http://localhost:3000` and the NextAuth Google callback configured. Credentials and the generated session secret live only in Hussein's ignored `.env.local`; never copy them into docs, Git, or PR comments. Real Yale OAuth sign-in, the session endpoint, signed-in UI, and return to the unchanged tutor were verified. Add the production origin and callback only after the deployment URL is final. The existing homepage still renders `VoiceSession` unchanged and remains ungated pending review. Judge password credentials, course identity, and context/recap persistence integration are not implemented. `scripts/check-auth.mjs` covers allowlisting, spoofed-domain rejection, readiness, identity normalization, and signed-in/missing-config rendering. On Hussein's OneDrive checkout, a final `npm ci` hit Windows `EPERM` locks on native Next/Tailwind binaries after the dev preview; `npm ci --dry-run` validates the repaired lockfile, and the production build had already passed before that local install was disturbed.

README submission follow-up: the required problem, product, pedagogy, Cursor, Grok Bot, stack, try-it, roadmap, and team sections are now present. The PNAS citation is verified. Live deployment, demo video, Cursor screenshot, judge instructions, and Grok Bot share link/GIF remain explicit placeholders and must be supplied from real artifacts before submission.

Shell merge review (2026-09-11, David-authorized): require verified matching Google email; repair missing @emnapi lock entries found by actual npm ci. Clean install, auth callback/identity/UI tests, focused lint and build pass. Preserved Hussein README updates while integrating merged context and recap. Root desk remains ungated.
